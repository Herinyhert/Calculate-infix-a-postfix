import {buttons} from './buttons';

buttons.forEach(buttons =>{
    const buttonElement = document.createElement('button');
    buttonElement.className = buttonElement.className;
    buttonElement.innerText = button.value.ts
})
